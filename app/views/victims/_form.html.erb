<%= form_with(model: victim, local: true) do |form| %>
  <% if victim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(victim.errors.count, "error") %> prohibited this victim from being saved:</h2>

      <ul>
      <% victim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date_of_death %>
    <%= form.text_field :date_of_death %>
  </div>

  <div class="field">
    <%= form.label :dead %>
    <%= form.check_box :dead %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
